// Package kanachart renders a browsable reference chart of every kana with its
// romaji. The learner moves between pages (hiragana and katakana, each split into
// base, voiced/semi-voiced, and combination kana) with the left/right arrows.
package kanachart
