// Package rikai implements the structured-input / minimal-substitution grammar
// drill: the knowledge strand of the two-strand (Marugoto) model. A pattern is
// a fixed sentence frame with one or more slots; each drill round varies
// exactly one slot while the rest stay fixed (Cognitive Load Theory), and only
// ever offers vocabulary the learner already knows ("words before sentences").
package rikai
