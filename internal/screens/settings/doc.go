// Package settings implements the settings screen. Its first action is a
// destructive "delete all app data" option guarded by an explicit confirmation;
// the actual deletion is performed by the router via nav.WipeData.
package settings
