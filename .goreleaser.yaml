version: 2

project_name: polyglot

before:
  hooks:
    - go mod tidy

builds:
  - id: polyglot
    main: ./cmd/polyglot
    binary: polyglot
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{ .Version }}

archives:
  - id: default
    formats:
      - tar.gz
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    format_overrides:
      - goos: windows
        formats:
          - zip

checksum:
  name_template: checksums.txt

changelog:
  # No sort: keep chronological (newest-first) order from the git history.
  use: github
  filters:
    exclude:
      - "^test:"
      - "^chore:"
      - "^Initial commit"
      - "Merge pull request"
      - "Merge branch"

release:
  draft: false
  prerelease: auto
