[workspace]
resolver = "2"
members = ["core", "term"]

[workspace.package]
edition = "2021"
license = "MIT"
version = "0.0.3"
rust-version = "1.90"

[workspace.dependencies]
chrono = { version = "0.4", default-features = false, features = ["clock", "std"] }
